function maiorDeDoisNumeros(num1, num2) {
    if (num1 > num2) {
        console.log(`${num1} é maior que ${num2}`)
    } else {
        console.log(`${num2} é maior que ${num1}`)
    }
}

maiorDeDoisNumeros(509, 155)
